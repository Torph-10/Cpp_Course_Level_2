/*Register variables are variables that the compiler attempts 
to store in the CPU's registers rather than in main memory (RAM). 
This can lead to faster access times, 
as registers are significantly faster to access than RAM. 
The register keyword is used to declare a register variable.*/ 

// cuz register is faster and the nearby place to CPU.